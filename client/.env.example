# Local dev: Vite proxy forwards /api → FastAPI on port 8000 (no change needed)
# For production (Render/Railway/Vercel), set this to your deployed FastAPI URL.
VITE_API_URL=https://janvaani-ai.onrender.com
